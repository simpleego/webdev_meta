<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.bookstore.order.mapper.OrderMapper">

    <!-- 주문 저장 -->
    <insert id="save" parameterType="com.bookstore.order.entity.Order">
        INSERT INTO Orders (custid, bookid, saleprice, orderdate, quantity)
        VALUES (#{custid}, #{bookid}, #{saleprice}, #{orderdate}, #{quantity})
    </insert>

<!--    &lt;!&ndash; 특정 회원의 주문 내역 조회 &ndash;&gt;-->
<!--    <select id="findByCustId" parameterType="int" resultType="com.example.model.Order">-->
<!--        SELECT * FROM Orders WHERE custid = #{custid}-->
<!--    </select>-->

<!--    &lt;!&ndash; 사용자 이름으로 주문 정보 조회 &ndash;&gt;-->
<!--    <select id="findOrderInfoByUserName" parameterType="String" resultType="com.example.model.Order">-->
<!--        SELECT b.bookname, b.price, o.orderdate-->
<!--        FROM orders o-->
<!--                 JOIN book b ON o.bookid = b.bookid-->
<!--        WHERE o.custid = (-->
<!--            SELECT u.username-->
<!--            FROM users u-->
<!--            WHERE u.name = #{name}-->
<!--        )-->
<!--    </select>-->

</mapper>