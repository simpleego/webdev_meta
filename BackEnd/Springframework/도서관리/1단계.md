# 1단계
- JSP를 View로 사용하며, 도서에 대한 **검색, 목록, 수정, 삭제, 등록** 기능을 구현
---

## 🧱 기본 구조

### 📁 프로젝트 구조 (Maven 기반 예시)
```
bookstore/
├── src/
│   ├── main/
│   │   ├── java/
│   │   │   └── com.example.bookstore/
│   │   │       ├── controller/
│   │   │       ├── service/
│   │   │       ├── repository/
│   │   │       └── model/
│   │   ├── resources/
│   │   │   └── application.properties
│   │   └── webapp/
│   │       └── WEB-INF/
│   │           └── views/
│   │               ├── list.jsp
│   │               ├── form.jsp
│   │               └── search.jsp
└── pom.xml
```

---

## 🗃️ DB 테이블 (Book)

```sql
CREATE TABLE Book (
    bookid INT PRIMARY KEY,
    bookname VARCHAR(100),
    publisher VARCHAR(100),
    price INT
);
```

---

## 🧩 Model 클래스

```java
public class Book {
    private int bookid;
    private String bookname;
    private String publisher;
    private int price;

    // Getters and Setters
}
```

---

## 📦 Repository (DAO)

```java
@Repository
public class BookRepository {
    @Autowired
    private JdbcTemplate jdbcTemplate;

    public List<Book> findAll() {
        return jdbcTemplate.query("SELECT * FROM Book", new BeanPropertyRowMapper<>(Book.class));
    }

    public Book findById(int id) {
        return jdbcTemplate.queryForObject("SELECT * FROM Book WHERE bookid = ?", new BeanPropertyRowMapper<>(Book.class), id);
    }

    public List<Book> search(String keyword) {
        String sql = "SELECT * FROM Book WHERE bookname LIKE ?";
        return jdbcTemplate.query(sql, new BeanPropertyRowMapper<>(Book.class), "%" + keyword + "%");
    }

    public void save(Book book) {
        String sql = "INSERT INTO Book (bookid, bookname, publisher, price) VALUES (?, ?, ?, ?)";
        jdbcTemplate.update(sql, book.getBookid(), book.getBookname(), book.getPublisher(), book.getPrice());
    }

    public void update(Book book) {
        String sql = "UPDATE Book SET bookname=?, publisher=?, price=? WHERE bookid=?";
        jdbcTemplate.update(sql, book.getBookname(), book.getPublisher(), book.getPrice(), book.getBookid());
    }

    public void delete(int id) {
        jdbcTemplate.update("DELETE FROM Book WHERE bookid = ?", id);
    }
}
```

---

## 🎯 Controller

```java
@Controller
@RequestMapping("/books")
public class BookController {

    @Autowired
    private BookRepository bookRepository;

    @GetMapping
    public String list(Model model) {
        model.addAttribute("books", bookRepository.findAll());
        return "list";
    }

    @GetMapping("/search")
    public String search(@RequestParam("keyword") String keyword, Model model) {
        model.addAttribute("books", bookRepository.search(keyword));
        return "list";
    }

    @GetMapping("/new")
    public String newForm(Model model) {
        model.addAttribute("book", new Book());
        return "form";
    }

    @PostMapping("/save")
    public String save(@ModelAttribute Book book) {
        bookRepository.save(book);
        return "redirect:/books";
    }

    @GetMapping("/edit/{id}")
    public String editForm(@PathVariable int id, Model model) {
        model.addAttribute("book", bookRepository.findById(id));
        return "form";
    }

    @PostMapping("/update")
    public String update(@ModelAttribute Book book) {
        bookRepository.update(book);
        return "redirect:/books";
    }

    @GetMapping("/delete/{id}")
    public String delete(@PathVariable int id) {
        bookRepository.delete(id);
        return "redirect:/books";
    }
}
```

---

## 🖼️ JSP 예시

### 📄 list.jsp
```jsp
<%@ page contentType="text/html;charset=UTF-8" %>
<html>
<head><title>도서 목록</title></head>
<body>
<h2>도서 목록</h2>
<form action="/books/search" method="get">
    <input type="text" name="keyword" placeholder="도서명 검색"/>
    <button type="submit">검색</button>
</form>
<a href="/books/new">도서 등록</a>
<table border="1">
    <tr><th>ID</th><th>도서명</th><th>출판사</th><th>가격</th><th>관리</th></tr>
    <c:forEach var="book" items="${books}">
        <tr>
            <td>${book.bookid}</td>
            <td>${book.bookname}</td>
            <td>${book.publisher}</td>
            <td>${book.price}</td>
            <td>
                <a href="/books/edit/${book.bookid}">수정</a>
                <a href="/books/delete/${book.bookid}">삭제</a>
            </td>
        </tr>
    </c:forEach>
</table>
</body>
</html>
```

### 📄 form.jsp
```jsp
<%@ page contentType="text/html;charset=UTF-8" %>
<html>
<head><title>도서 등록/수정</title></head>
<body>
<h2>도서 정보</h2>
<form action="${book.bookid == 0 ? '/books/save' : '/books/update'}" method="post">
    <input type="hidden" name="bookid" value="${book.bookid}"/>
    도서명: <input type="text" name="bookname" value="${book.bookname}"/><br/>
    출판사: <input type="text" name="publisher" value="${book.publisher}"/><br/>
    가격: <input type="number" name="price" value="${book.price}"/><br/>
    <button type="submit">저장</button>
</form>
</body>
</html>
```

---

## 🛠️ application.properties

```properties
spring.datasource.url=jdbc:mysql://localhost:3306/bookdb
spring.datasource.username=root
spring.datasource.password=yourpassword
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.mvc.view.prefix=/WEB-INF/views/
spring.mvc.view.suffix=.jsp
```

---

이제 1단계 도서 관리 기능이 완성됐습니다!  
다음 단계로 주문(Order) 및 고객(Customer) 기능도 확장할 수 있어요. 원하시면 이어서 설계해드릴게요. 😄
