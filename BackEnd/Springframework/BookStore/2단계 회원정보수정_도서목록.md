# 도서목록, 회원정보 수정

> **로그인한 사용자를 위한 도서 목록을 표시**하고,  
> **회원 정보를 수정할 수 있는 기능을 추가**하겠습니다.


-----

### 📁 1단계: 도서 관련 기능 추가

먼저 도서 정보를 다루기 위한 `Domain`, `Repository`, `Service`, `Controller`를 추가합니다.

#### **1. Domain: `Book.java`**

DB의 `Book` 테이블과 매핑될 객체입니다.

```java
package com.example.project.domain;

import lombok.Data;

@Data
public class Book {
    private Long bookid;
    private String bookname;
    private String publisher;
    private Integer price;
}
```

#### **2. Repository: `BookRepository.java`**

`JdbcTemplate`을 사용하여 DB에서 모든 도서 목록을 조회합니다.

```java
package com.example.project.repository;

import com.example.project.domain.Book;
import org.springframework.jdbc.core.JdbcTemplate;
import org.springframework.jdbc.core.RowMapper;
import org.springframework.stereotype.Repository;

import javax.sql.DataSource;
import java.util.List;

@Repository
public class BookRepository {

    private final JdbcTemplate jdbcTemplate;

    public BookRepository(DataSource dataSource) {
        this.jdbcTemplate = new JdbcTemplate(dataSource);
    }

    private RowMapper<Book> bookRowMapper() {
        return (rs, rowNum) -> {
            Book book = new Book();
            book.setBookid(rs.getLong("bookid"));
            book.setBookname(rs.getString("bookname"));
            book.setPublisher(rs.getString("publisher"));
            book.setPrice(rs.getInt("price"));
            return book;
        };
    }

    // 모든 도서 목록 조회
    public List<Book> findAll() {
        String sql = "SELECT * FROM Book";
        return jdbcTemplate.query(sql, bookRowMapper());
    }
}
```

#### **3. Service: `BookService.java`**

도서 관련 비즈니스 로직을 처리합니다. 지금은 단순히 Repository를 호출하는 역할만 합니다.

```java
package com.example.project.service;

import com.example.project.domain.Book;
import com.example.project.repository.BookRepository;
import org.springframework.stereotype.Service;

import java.util.List;

@Service
public class BookService {

    private final BookRepository bookRepository;

    public BookService(BookRepository bookRepository) {
        this.bookRepository = bookRepository;
    }

    public List<Book> findBooks() {
        return bookRepository.findAll();
    }
}
```

#### **4. Controller: `BookController.java`**

도서 목록 페이지 요청을 처리합니다.

```java
package com.example.project.controller;

import com.example.project.domain.Book;
import com.example.project.service.BookService;
import org.springframework.stereotype.Controller;
import org.springframework.ui.Model;
import org.springframework.web.bind.annotation.GetMapping;

import java.util.List;

@Controller
public class BookController {

    private final BookService bookService;

    public BookController(BookService bookService) {
        this.bookService = bookService;
    }

    @GetMapping("/books")
    public String list(Model model) {
        List<Book> books = bookService.findBooks();
        model.addAttribute("books", books);
        return "books/bookList"; // templates/books/bookList.html
    }
}
```

-----

### 📁 2단계: 회원 정보 수정 기능 추가

기존의 `MemberController`, `MemberService`, `CustomerRepository`에 정보 수정 로직을 추가하고, 관련 화면을 만듭니다.

#### **1. Repository: `CustomerRepository.java` 수정**

회원 정보를 업데이트하는 `update` 메서드를 추가합니다.

```java
// CustomerRepository.java 파일에 아래 메서드를 추가합니다.

public void update(Customer customer) {
    String sql = "UPDATE Customer SET name = ?, password = ?, address = ?, phone = ? WHERE custid = ?";
    jdbcTemplate.update(sql,
            customer.getName(),
            customer.getPassword(),
            customer.getAddress(),
            customer.getPhone(),
            customer.getCustid());
}
```

#### **2. Service: `MemberService.java` 수정**

정보 수정을 위한 `updateMember` 메서드를 추가합니다.

```java
// MemberService.java 파일에 아래 메서드를 추가합니다.

/**
 * 회원 정보 수정
 */
public void updateMember(Customer customer) {
    customerRepository.update(customer);
}
```

#### **3. Controller: `MemberController.java` 수정**

회원 정보 수정 폼으로 이동하고, 수정 요청을 처리하는 메서드를 추가합니다.

```java
// MemberController.java 파일에 아래 메서드들을 추가합니다.
import org.springframework.ui.Model; // Model import 추가
import jakarta.servlet.http.HttpSession; // HttpSession import 추가

//... 기존 코드 생략

// 회원 정보 수정 폼으로 이동
@GetMapping("/members/edit")
public String editForm(HttpSession session, Model model) {
    Customer loginMember = (Customer) session.getAttribute("loginMember");
    if (loginMember == null) {
        return "redirect:/"; // 로그인 안되어 있으면 홈으로
    }
    model.addAttribute("customer", loginMember);
    return "members/editMemberForm";
}

// 회원 정보 수정 처리
@PostMapping("/members/edit")
public String update(CustomerForm form, HttpSession session, RedirectAttributes redirectAttributes) {
    Customer loginMember = (Customer) session.getAttribute("loginMember");
    if (loginMember == null) {
        return "redirect:/";
    }

    // 폼에서 받은 정보로 세션의 회원 정보 업데이트
    loginMember.setName(form.getName());
    loginMember.setPassword(form.getPassword()); // 실제 프로젝트에서는 비밀번호 암호화 필요
    loginMember.setAddress(form.getAddress());
    loginMember.setPhone(form.getPhone());

    memberService.updateMember(loginMember);

    // 세션 정보도 업데이트
    session.setAttribute("loginMember", loginMember);

    redirectAttributes.addFlashAttribute("successMessage", "회원 정보가 성공적으로 수정되었습니다.");
    return "redirect:/";
}
```

-----

### 🖥️ 3단계: 화면(View) 수정 및 추가

로그인했을 때 도서 목록과 정보 수정 링크가 보이도록 `home.html`을 수정하고, 정보 수정 폼을 새로 만듭니다.

#### **1. View: `home.html` 수정**

로그인 시 도서 목록을 보여주고, 회원 정보 수정 링크를 추가합니다.

```html
<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
<head>
    <title>도서 주문 시스템</title>
</head>
<body>
    <h1>📚 도서 주문 시스템</h1>
    <div th:if="${successMessage}" th:text="${successMessage}" style="color:blue;"></div>
    <hr>
    <div th:if="${session.loginMember == null}">
        <a href="/members/new">회원 가입</a>
        <a href="/members/login">로그인</a>
    </div>

    <div th:if="${session.loginMember != null}">
        <p><b th:text="${session.loginMember.name}"></b>님, 환영합니다!</p>
        <div>
            <a href="/members/edit">회원정보 수정</a>
            <form th:action="@{/members/logout}" method="post" style="display:inline;">
                <button type="submit">로그아웃</button>
            </form>
        </div>

        <hr>

        <h3>🛒 구매 가능 도서 목록</h3>
        <table border="1">
            <thead>
                <tr>
                    <th>책 제목</th>
                    <th>출판사</th>
                    <th>가격</th>
                </tr>
            </thead>
            <tbody>
                <tr th:each="book : ${books}">
                    <td th:text="${book.bookname}"></td>
                    <td th:text="${book.publisher}"></td>
                    <td th:text="${book.price}"></td>
                </tr>
            </tbody>
        </table>
    </div>
</body>
</html>
```

#### **2. Controller: `HomeController.java` 수정**

로그인 상태일 때 도서 목록을 조회하여 `home.html`에 전달하도록 수정합니다.

```java
package com.example.project.controller;

import com.example.project.domain.Book;
import com.example.project.service.BookService;
import jakarta.servlet.http.HttpSession;
import org.springframework.stereotype.Controller;
import org.springframework.ui.Model;
import org.springframework.web.bind.annotation.GetMapping;
import java.util.List;

@Controller
public class HomeController {

    private final BookService bookService;

    public HomeController(BookService bookService) {
        this.bookService = bookService;
    }

    @GetMapping("/")
    public String home(HttpSession session, Model model) {
        // 로그인 상태일 경우에만 도서 목록을 모델에 추가
        if (session.getAttribute("loginMember") != null) {
            List<Book> books = bookService.findBooks();
            model.addAttribute("books", books);
        }
        return "home";
    }
}
```

#### **3. View: `editMemberForm.html` 추가**

`resources/templates/members/` 경로에 회원 정보 수정 폼을 추가합니다.

```html
<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
<body>
<div class="container">
    <h2>회원 정보 수정</h2>
    <form action="/members/edit" method="post">
        <div>
            <label>아이디</label>
            <input type="text" name="username" th:value="${customer.username}" readonly>
        </div>
        <div>
            <label>이름</label>
            <input type="text" name="name" th:value="${customer.name}" required>
        </div>
        <div>
            <label>비밀번호</label>
            <input type="password" name="password" th:value="${customer.password}" required>
        </div>
        <div>
            <label>주소</label>
            <input type="text" name="address" th:value="${customer.address}">
        </div>
        <div>
            <label>전화번호</label>
            <input type="text" name="phone" th:value="${customer.phone}">
        </div>
        <button type="submit">수정하기</button>
        <button type="button" onclick="location.href='/'">취소</button>
    </form>
</div>
</body>
</html>
```

-----

### ✅ 4단계: 실행 및 확인

1.  **DB에 Book 테이블과 데이터를 추가합니다.** (제공된 이미지 참고)
2.  프로젝트를 실행하고 로그인합니다.
3.  **메인 화면**에 "OOO님, 환영합니다\!" 메시지와 함께 **도서 목록** 및 **회원정보 수정 링크**가 나타나는지 확인합니다.
4.  **회원정보 수정** 링크를 클릭하면 기존 정보가 채워진 폼이 나타나는지 확인합니다.
5.  정보를 수정한 후 '수정하기' 버튼을 누르면, 메인 화면으로 돌아오면서 성공 메시지가 뜨고, 변경된 이름으로 환영 메시지가 바뀌는지 확인합니다.
